#include <iostream>
#include<cmath>
#include<fstream>
#include <algorithm>


using namespace std;
#define N 32


int main(void){

    int i, n_ciclos, N;
    float lambda, alpha[N], beta[N], xi[N][N], phi[N][N],s, v[N];

    //Vamos ainiciar la matriz de la funcion de onda

    for ( i = 0; i < N; i++)
    {
        
    }
    



    return 0;
}